#include <stdlib.h>
#include <stdio.h>


int main(int argc, char *argv[])
{
	// TODO - criar função separada para diretorio e abrir
	/*

	 DIR* dir;
	 dirp = opendir(argv[1]);

    if (dirp == NULL)
    {
        fprinft(stderr, "Error:: Invalid directory path. Did you forget the '/' at end?", strerror(erro));
        return 2;
    }

	 */


	// TODO apenas para verificar se está a entrar em lsdir
	fprintf(stdout, "entrou em lsdir");




	//perror(strerror(errno));
	//perror("Error getting data about directory.");

	return 0;
} 
